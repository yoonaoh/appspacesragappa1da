name: Trigger auto deployment for container-app-lmr7wbdmsh3j4

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - 'server/**'
    - '.github/workflows/container-app-lmr7wbdmsh3j4-AutoDeployTrigger-5c3d0083-2490-4b51-8641-340107757d96.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPLMR7WBDMSH3J4_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPLMR7WBDMSH3J4_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPLMR7WBDMSH3J4_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }}/server 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPLMR7WBDMSH3J4_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPLMR7WBDMSH3J4_REGISTRY_PASSWORD }}
          containerAppName: container-app-lmr7wbdmsh3j4
          resourceGroup: appspacesragappa1daacac
          imageToBuild: default/container-app-lmr7wbdmsh3j4:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


